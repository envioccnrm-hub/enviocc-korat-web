# วิธีอัปเดตเว็บ — อ่านตรงนี้ก่อน

สรุปสั้นสำหรับเวลาลืม (ฉบับเต็มอยู่ใน `README.md` ข้อ 7 และ 8)

---

## 1. แก้โค้ดหน้าเว็บ → ต้องทำ 3 อย่างตามลำดับ

```bash
./bump.sh                       # 1) ขยับเลขรุ่น (สำคัญมาก ห้ามลืม)
git add -A && git commit -m "อธิบายว่าแก้อะไร"
git push origin main            # 2) นี่คือการ deploy ของหน้าเว็บ
```

3) รอ **1–2 นาที** ให้ GitHub Pages อัปเดต แล้วเปิดเว็บกดรีเฟรช

> **ถ้าลืม `./bump.sh`** ผู้ใช้จะยังเห็นโค้ดเก่าจาก cache ทั้งที่ push ขึ้นไปแล้ว
> — เป็นสาเหตุอันดับหนึ่งของอาการ "แก้แล้วแต่เว็บไม่เปลี่ยน"

---

## 2. เช็คว่าได้โค้ดใหม่จริงไหม

เปิด Console แล้วดูบรรทัดสีน้ำเงิน:

```
[ระบบติดตามมาตรฐานโรงพยาบาล] หน้าเว็บรุ่น 2026-08-24h
```

| เบราว์เซอร์ | เปิด Console | รีเฟรชแบบล้าง cache |
|---|---|---|
| Chrome | `Cmd + Option + J` | `Cmd + Shift + R` |
| Safari | `Cmd + Option + C` | `Cmd + Option + R` |

- เลขตรงกับที่เพิ่ง bump = **ได้ของใหม่แล้ว** ✅
- เลขเป็นรุ่นเก่า = ยังติด cache ให้กดรีเฟรชแบบล้าง cache

*(Safari ต้องเปิด Develop menu ก่อน: Safari → Settings → Advanced → ติ๊ก "Show features for web developers")*

---

## 3. หน้าเว็บ กับ หลังบ้าน อัปเดตคนละทาง

| แก้ไฟล์ไหน | ต้องทำอะไร |
|---|---|
| `index.html`, `hospital.html`, `admin.html`, `test.html`, `assets/*.js` | `./bump.sh` → commit → **push** (จบแค่นี้) |
| `Code.gs` | เอาโค้ดไปวางใน Google Apps Script → **Deploy → Manage deployments → แก้ไข → Version: New version** |

> push ขึ้น GitHub **ไม่มีผล** กับ `Code.gs` ที่รันอยู่จริง และการ Deploy ที่ Apps Script
> ก็ **ไม่มีผล** กับหน้าเว็บ — แยกกันเด็ดขาด

---

## 4. คำเตือนใน Console ที่ปล่อยไว้ได้ (ไม่ใช่ปัญหา)

```
cdn.tailwindcss.com should not be used in production
```

แค่บอกว่าโหลด CSS จาก CDN แล้วคำนวณสดทุกครั้ง ทำให้ช้ากว่านิดหน่อย
**ระบบทำงานถูกต้องทุกอย่าง** — การเอาออกต้องเพิ่มขั้นตอน build ซึ่งขัดกับที่ตั้งใจ
ให้โปรเจกต์นี้แก้ไฟล์แล้ว push ได้เลย จึงเลือกปล่อยไว้

---

## 5. ลิงก์ที่ใช้บ่อย

- เว็บจริง: https://envioccnrm-hub.github.io/enviocc-korat-web/
- หน้าตรวจสอบการเชื่อมต่อ: https://envioccnrm-hub.github.io/enviocc-korat-web/test.html
- GitHub repo: https://github.com/envioccnrm-hub/enviocc-korat-web

---

## 6. เปิดดูบนเครื่องตัวเองก่อน push

```bash
cd "โฟลเดอร์ของโปรเจกต์นี้"
python3 -m http.server 8080
```

แล้วเปิด http://localhost:8080/index.html

> ⚠️ ห้ามดับเบิลคลิกเปิดไฟล์ตรงๆ (`file://...`) เบราว์เซอร์จะบล็อกการเรียก API
